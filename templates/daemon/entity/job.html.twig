{% extends 'base.html.twig' %}

{% block main %}
    {{ gabarit.SuivantPrecedent(offset, limit, count, path('app.legacy.entite_job', {'filtre': filtre, 'id_e': id_e})) }}
    {% include 'daemon/job_list.html.twig' %}
    {{ gabarit.SuivantPrecedent(offset, limit, count, path('app.legacy.entite_job', {'filtre': filtre, 'id_e': id_e})) }}
{% endblock %}