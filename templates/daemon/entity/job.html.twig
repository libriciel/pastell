{% extends 'base.html.twig' %}

{% block main %}
    {% include 'daemon/job_search_form.html.twig' %}

    {% set pagination_params = {
        'filtre': filtre,
        'id_e': id_e,
        'job_type': search.type,
        'job_status': search.job_status,
        'suspended': search.suspended,
        'search_id_e': search.id_e,
        'include_children': search.include_children,
        'id_verrou': search.id_verrou
    } %}

    {{ gabarit.SuivantPrecedent(offset, limit, count, path('app.legacy.entite_job', pagination_params)) }}
    {% include 'daemon/job_list.html.twig' %}
    {{ gabarit.SuivantPrecedent(offset, limit, count, path('app.legacy.entite_job', pagination_params)) }}
{% endblock %}
