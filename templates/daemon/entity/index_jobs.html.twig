<div id="daemon-jobs-inner">
    {% set pagination_params = {
        'id_e': id_e,
        'type': search.type,
        'job_status': search.job_status,
        'suspended': search.suspended,
        'search_id_e': search.id_e,
        'id_verrou': search.id_verrou
    } %}

    {{ gabarit.SuivantPrecedent(offset, limit, count, path('app.legacy.entite_daemon', pagination_params)) }}
    {% include 'daemon/job_list.html.twig' %}
    {{ gabarit.SuivantPrecedent(offset, limit, count, path('app.legacy.entite_daemon', pagination_params)) }}
</div>
